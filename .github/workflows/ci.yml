name: Docker
on:
  push:
    branches: [ "main" ]
  workflow_dispatch:
env:
  IMAGE_NAME: liuzhen932/app-blessing-skin-server

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@4d04d5d9486b7bd6fa91e7baf45bbb4f8b9deedd # v4

      - name: Log into registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@4907a6ddec9925e35a0a9e82d7399ccc52663121 # v4
        with:
          registry: docker.io
          username: liuzhen932
          password: ${{ secrets.DOCKER_TOKEN }}

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@dc802804100637a589fabce1cb79ff13a1411302 # v6
        with:
          images: docker.io/${{ env.IMAGE_NAME }}
          tags: |
            latest
            php84

      - name: Download source
        run: |
          sudo apt-get install wget || true
          wget -O srccc.zip https://github.com/liuzhen9320/blessing-skin-server/releases/download/dev/blessing-skin-server-16f46b5.zip
          unzip -o srccc.zip -d ./src
          ls -all ./src

      - name: Build & Push Docker image
        uses: docker/build-push-action@f9f3042f7e2789586610d6e8b85c8f03e5195baf # v7
        with:
          context: .
          file: Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
